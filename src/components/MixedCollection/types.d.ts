import { MixedCollectionItemsProps } from "../MixedCollectionItem/types";

export interface MixedCollectionProps {
    // subHeading: string;
    // className:string;
    // image:{imageSrc,altText}
    items: MixedCollectionItemsProps[];
    
}